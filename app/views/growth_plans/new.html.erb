<%= render "pages/sidenav" %>

<div class="p-4 sm:ml-64">
  <div class="p-4 border-2 border-gray-200 border-dashed rounded-lg dark:border-gray-700">


    <h1 class="font-bold text-4xl mb-6">New Growth Plan</h1>

    <div class="w-1/2 mx-auto">
      <%= form_with(model: @growth_plan, local: true, class: "rounded-lg") do |f| %>
          <% if @growth_plan.errors.any? %>
            <div class="bg-red-50 p-4 rounded-lg mb-6">
              <h2 class="text-red-800 font-medium"><%= pluralize(@growth_plan.errors.count, "error") %> prohibited this growth plan from being saved:</h2>
              <ul class="list-disc list-inside text-red-600">
                <% @growth_plan.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="space-y-4 mb-6">
            <div>
              <%= f.label :title, class: "block text-sm font-medium text-gray-700" %>
              <%= f.text_field :title, 
                  class: "block w-full rounded-lg border border-gray-300 bg-gray-50 p-2.5 text-sm text-gray-900 focus:border-blue-500 focus:ring-blue-500",
                  placeholder: "Enter the title of your growth plan" %>
            </div>

            <div>
              <%= f.label :description, class: "block text-sm font-medium text-gray-700" %>
              <%= f.text_area :description, 
                  rows: 4,
                  class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm",
                  placeholder: "Describe your growth plan objectives..." %>
            </div>


            <div>
              <%= f.label :status, class: "block text-sm font-medium text-gray-700" %>
              <%= f.select :status, 
                  ['draft', 'active', 'completed'], 
                  { include_blank: 'Select status' },
                  class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
            </div>
          </div>

          <div class="flex justify-end space-x-3">
            <%= link_to "Cancel", growth_plans_path, 
                class: "px-4 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md shadow-sm hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
            
            <%= f.submit "Create Growth Plan", 
                class: "px-4 py-2 text-sm font-medium text-white bg-indigo-600 border border-transparent rounded-md shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
          </div>
      <% end %>
    </div>



</div>
